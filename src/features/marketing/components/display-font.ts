// Bricolage Grotesque, set against the app's Plus Jakarta Sans for body copy.
// The face is loaded by the marketing home page; this constant is split out so
// client leaves can wear it without pulling `next/font` into their bundle.
export const DISPLAY = 'font-[family-name:var(--font-display)]';
