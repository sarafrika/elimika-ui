import { NewSchedulerCalendarPage } from '@/app/dashboard/_components/calendar/new-scheduler/new-scheduler-calendar-page';

const Page = () => {
  return <NewSchedulerCalendarPage profile='admin' />;
};

export default Page;
