/**
 * Organisation dashboard shares the refined-neutral admin foundation. Re-exported
 * from a local barrel so org pages import from `../_components/ui` and the components
 * can later be promoted to a neutral `components/dashboard/` without touching consumers.
 */
export * from '@/app/dashboard/@admin/_components/ui';
