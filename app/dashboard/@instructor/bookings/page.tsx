'use client';

import { useInstructor } from '@/context/instructor-context';

function BookingsPage() {
  const _instructor = useInstructor();
  // const {data} = useQuery({})

  return <div>This page is under construction

    <p>Manage bookings - instructors shoud be able to accept or reject bookings</p>
  </div>;
}

export default BookingsPage;
