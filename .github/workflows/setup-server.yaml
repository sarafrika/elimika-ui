name: 'Setup Server'

on:
  workflow_dispatch:

jobs:
  setup_server:
    name: Setup Server
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Create & Transfer .env.local to Server
        run: |
          echo "${{ secrets.SERVER_SSH_KEY }}" > private_key
          chmod 600 private_key

          echo "🔑 Creating .env.local file from template..."
          # Copy sample env file and populate with secrets
          cp .env.sample .env.local

          # Replace placeholders with actual secret values
          sed -i "s|__API_BASE_URL__|${{ secrets.API_BASE_URL }}|g" .env.local
          sed -i "s|__AUTH_URL__|${{ secrets.NEXTAUTH_URL }}|g" .env.local
          sed -i "s|__AUTH_SECRET__|${{ secrets.NEXTAUTH_SECRET }}|g" .env.local
          sed -i "s|__KEYCLOAK_ISSUER__|${{ secrets.KEYCLOAK_ISSUER }}|g" .env.local
          sed -i "s|__KEYCLOAK_CLIENT_ID__|${{ secrets.KEYCLOAK_CLIENT_ID }}|g" .env.local
          sed -i "s|__KEYCLOAK_CLIENT_SECRET__|${{ secrets.KEYCLOAK_CLIENT_SECRET }}|g" .env.local
          sed -i "s|__KEYCLOAK_REALM__|${{ secrets.KEYCLOAK_REALM }}|g" .env.local

          echo "📤 Transferring .env.local to server..."
          scp -i private_key -o StrictHostKeyChecking=no .env.local "${{ secrets.SERVER_SSH_USER }}@${{ secrets.SERVER_IP }}":~/products/elimika-ui/.env.local
          echo "✅ .env.local file transferred!"

      - name: Transfer Docker Compose File & Setup Server
        run: |
          echo "${{ secrets.SERVER_SSH_KEY }}" > private_key
          chmod 600 private_key

          echo "📂 Ensuring deployment directory exists on server..."
          ssh -i private_key -o StrictHostKeyChecking=no "${{ secrets.SERVER_SSH_USER }}@${{ secrets.SERVER_IP }}" "mkdir -p ~/products/elimika-ui/docker"

          echo "📤 Transferring docker-compose.yaml to server..."
          scp -i private_key -o StrictHostKeyChecking=no docker/compose.yaml "${{ secrets.SERVER_SSH_USER }}@${{ secrets.SERVER_IP }}":~/products/elimika-ui/docker/compose.yaml

          echo "✅ Server setup configuration complete!"
